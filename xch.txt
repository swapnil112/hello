import pandas as pd
from sklearn.metrics import classification_report

# generate classification report for y_pred and y_real
report_pred_real = classification_report(y_real, y_pred, output_dict=True)
report_pred_real_df = pd.DataFrame(report_pred_real).transpose()
report_pred_real_df.index.name = "Dataset (y_pred vs y_real)"

# generate classification report for y_com and y_real
report_com_real = classification_report(y_real, y_com, output_dict=True)
report_com_real_df = pd.DataFrame(report_com_real).transpose()
report_com_real_df.index.name = "Dataset (y_com vs y_real)"

# combine the classification reports for all datasets
combined_report_df = pd.concat([report_pred_real_df, report_com_real_df])

# print the combined report
print("Combined classification report:\n", combined_report_df)
