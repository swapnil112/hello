from sklearn.metrics import classification_report
from sklearn.preprocessing import MultiLabelBinarizer

# combine all datasets into a list of tuples
all_data = list(zip(y_real, y_pred, y_com))

# create a MultiLabelBinarizer object
mlb = MultiLabelBinarizer()

# convert all datasets to have multi-label indicators
y_real_mlb = mlb.fit_transform([[label] for label in y_real])
y_pred_mlb = mlb.transform([[label] for label in y_pred])
y_com_mlb = mlb.transform([[label] for label in y_com])

# generate classification report for all datasets
report_all = classification_report(y_real_mlb, y_pred_mlb, labels=mlb.classes_, output_dict=True)

# convert the classification report to a DataFrame and add dataset names as index
report_all_df = pd.DataFrame(report_all).transpose()
report_all_df.index.name = "Dataset (y_pred vs y_real vs y_com)"

# print the classification report for all datasets
print("Classification report for all datasets:\n", report_all_df)
